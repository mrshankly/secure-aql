-module(join).

-include("parser.hrl").
-include("aql.hrl").

-export([scan/3]).

%scan(Table, ?PARSER_WILDCARD, TxId, Table2) ->
%  TName = table:name(Table),
%  TName2 = table:name(Table2),
%   lists:map(fun({_Key, BObj}) -> BObj end, TName2);

%scan(Table, Conditions, TxId) ->
%evaluate(Table, Conditions, TxId, []).

%% ====================================================================
%% Internal functions
%% ====================================================================


%evaluate(Table, [], TxId, Acc) ->
 % Acc. 


